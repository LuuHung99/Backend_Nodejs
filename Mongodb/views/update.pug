<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Document
  body
    form(action=`/update/${value[0].id}`, method="POST")
      input(type="text", name="title" value=value[0].title)
      input(type="text", name="status" value=value[0].status)
      button(type="submit") update