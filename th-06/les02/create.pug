<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Document
  body
    h1=title
    from(action="create" method="POST")
      input(name="title")
      select(name="stutus")
        option(value="New") New
        option(value="Done") Done
        option(value="Progess") Progess
      input(type="submit" value="SUBMIT")
